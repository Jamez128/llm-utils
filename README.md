1. llm-utils

A tiny shared Python utility module: exponential backoff retry decorator for LLM API calls, and a prompt caching wrapper that prefixes a cached system prompt block. No dependencies beyond the Anthropic/OpenAI SDK. This gets imported by every project that follows — build it once, use it everywhere.

~1-2 hrs · Work hours · Standalone

## Setup
